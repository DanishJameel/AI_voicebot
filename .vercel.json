{
  "builds": [
    {
      "src": "react_template/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "react_template/dist" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
